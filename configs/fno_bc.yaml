experiment:
  name: fno_bc
  root_dir: experiments/fno_bc
  seed: 42

dataset:
  root: datasets/bc
  seq_len: 20

model:
  modes: 16
  width: 64
  depth: 4

optimization:
  batch_size: 8
  learning_rate: 5.0e-4
  epochs: 300

checkpoint:
  save_every: 10
